apiVersion: v1
kind: Service
metadata:
  name: {{ include "configserver.fullname" . }}
  labels:
    {{- include "configserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "configserver.selectorLabels" . | nindent 4 }}
